#include <stdio.h>
#include <locale.h>

void main(){
    setlocale(LC_ALL,"");

    int num;
    scanf("%d", num)
        int soma = a + b;

    printf("O valor de soma entre %d e %d é %d", a, b, soma);
}
